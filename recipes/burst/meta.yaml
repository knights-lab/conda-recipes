{% set version = "v0.99.5a" %}

package:
  name: burst
  version: "{{ version }}"

source:
  fn: burst-{{ version }}-linux-64.tar.gz #[linux]
  url: https://github.com/knights-lab/BURST/releases/download/{{ version }}/burst-{{ version }}-linux-64.tar.gz #[linux]
  sha256: ddc8264bf9c960aefa896d0da15d6d940f1880c437cf5ac9bd4525ed4b8026ad   #[linux]

requirements:
  build:
  run:

about:
  summary: An ultrafast optimal aligner for mapping large NGS data to large genome databases.
  home: https://github.com/knights-lab/BURST
  license: AGPL
